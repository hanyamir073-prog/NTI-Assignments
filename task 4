#include <stdio.h>

 int max(int arr[4])   
   {
       int i,m=arr[0];
       for(i=0;i<4;i++){
        
if(arr[i]>m)
m=arr[i];
       
       }

return m;
   }

int min(int arr[4])
{
int i,m=arr[0];
for (i=0;i<4;i++){

if(arr[i]<m)
m=arr[i];
}
return m;
}

int main()
{
    int arr[4];
    int i;
 for (i=0;i<4;i++){
  printf ("Enter number=%d:",i);
  scanf("%d",&arr[i]);
 }
    
printf("Maxmum is %d \n",max(arr));

printf("Minmum is %d \n",min(arr));


return 0;
}











